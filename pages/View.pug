doctype html
head
  title Viewpeople_page
  link(rel='stylesheet' href='/css/style.css')
br
.topBar
  img(src='img/ilovem.jpg' alt='ilovem icion' width='150' height='80')
  hr
  a(href='http://localhost:3000') Home
  if session.loggedin
    a(href='/profile') Profile
  .searchBar
    if session.loggedin
      form(method="post" action="/people")
        label(for='peoName')
        input#movName(type='text' placeholder='Search people' name = 'peoName')
        button#search(type='submit') Search
    else
      form(method="post" action="/people")
        label(for='peoName')
        input#movName(type='text' placeholder='Search people' name = 'peoName')
        button#search.btn(type='submit') Search
        button#sign(onclick = "window.location.href= 'http://localhost:3000/signin'" type='button') Sign in
hr
.col
.midcol
  .row
    h2  Name of Person: #{name}
    p Discription of the person, ....
    h3 History
    a(href='movie')  Jumanji
    a(href='movie') Sudden Death
  .row
    h3  Collaborators
    p simon
  form(method="post" action= '/people/' + movName)
    button#subscribe(type='submit') subscribe
.col
